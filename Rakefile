desc 'Publicly release updated documentation'
task :release do
  # TODO:
  #
  # * push to github
  # * generate docs
  # * push to github pages
  # * deploy to heroku
  puts 'Not yet implemented'
end
